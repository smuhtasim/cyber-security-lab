Method: getMapper
VULNERABLE VERSION:

     public Mapper getMapper() {

         return (mapper);

     }
FIXED VERSION:

    public Mapper getMapper() {
        return (mapper);
    }
================================================================================
