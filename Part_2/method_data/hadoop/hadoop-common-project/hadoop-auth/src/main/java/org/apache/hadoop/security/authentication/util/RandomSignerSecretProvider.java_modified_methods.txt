Method: generateNewSecret
VULNERABLE VERSION:

  protected byte[] generateNewSecret() {
    return Long.toString(rand.nextLong()).getBytes(Charset.forName("UTF-8"));
  }
FIXED VERSION:

  protected byte[] generateNewSecret() {
    byte[] secret = new byte[32]; // 32 bytes = 256 bits
    rand.nextBytes(secret);
    return secret;
  }
================================================================================
